class GlobalConfig {
  GlobalConfig._();

  static const String appName = 'NoorLife';
  static const int maxRetryCount = 3;
  static const Duration defaultAnimationDuration = Duration(milliseconds: 300);
  static const bool enableNetworkLogging = true;
}